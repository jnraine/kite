<% if user_signed_in? %>
	<% unless current_user.flagged_events.not_over.size == 0 %>
		<div class="row">
			<%= link_to events_path(:favs => true) do %>
			<div class="event-list">
				Favourites
			</div>
			<% end %>
		</div>
	<% end %>
<% end %>